[package]
name = "text_oracle"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
clock = { path = "../../crates/clock" }
text = { path = "../../crates/text", features = ["test-support"] }
