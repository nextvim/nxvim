pub fn select_similar(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn begin_macro(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn replay_macro(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn delete_char(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn delete_char_before(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn resize_left(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn resize_right(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn resize_up(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn resize_down(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn join_lines(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn quit(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn change_case(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn fold(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn unfold(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn clear(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
pub fn sequence(editor: &mut Editor, window: WindowId, select: bool) -> Outcome {
    super::marks_and_jumps::record_jump(editor, window);
    let (win, buffer) = editor.window_and_buffer_mut(window);
    // TODO
    moved(editor, window, select)
}
